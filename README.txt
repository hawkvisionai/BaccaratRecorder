Baccarat Platform Studio v14.5｜牌桌位置版

本版調整：
- 完整記錄改為「閒在左、莊在右」
- 閒對／莊對、自然牌、牌數按鈕同步交換位置
- 只記勝方按鈕改為「閒、莊、和」
- 儲存後游標自動回到閒點數
- 最近牌局與牌靴內容顯示改為先閒後莊

更新方式：
1. 不需要執行新的 SQL。
2. 到 GitHub 覆蓋 index.html 與 app.js。
3. style.css 可維持原檔；整包覆蓋也可以。
4. 更新後重新整理網頁。

資料庫欄位與既有資料完全不變，只調整畫面位置與輸入順序。
