# Baccarat Platform Studio v16.7.2

## 介面調整

- 移除「拍照辨識」按鈕下方的使用說明文字。
- AI 完整記錄模式中，將「下一局」按鈕移至「拍照辨識」正下方。
- 手動模式與只記勝方模式仍保留原本的「下一局」位置。
- 不修改六格取景框、AI 裁切、辨識與 Supabase 後端邏輯。
- 更新 CSS/JS 快取版本為 v16.7.2。

部署只需覆蓋：`index.html`、`app.js`、`style.css`。
